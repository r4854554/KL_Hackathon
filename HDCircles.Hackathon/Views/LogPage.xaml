<Page
    x:Class="HDCircles.Hackathon.Views.LogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HDCircles.Hackathon.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mstk="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <mstk:DataGrid x:Name="LogGrid" AutoGenerateColumns="False">
            <mstk:DataGrid.Columns>
                <mstk:DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" />
                <mstk:DataGridTextColumn Header="Type Name" Binding="{Binding TypeName}" />
                <mstk:DataGridTextColumn Header="Thread Id" Binding="{Binding ThreadId}" />
                <mstk:DataGridTextColumn Header="Message" Binding="{Binding Message}" />
            </mstk:DataGrid.Columns>
        </mstk:DataGrid>
    </Grid>
</Page>
